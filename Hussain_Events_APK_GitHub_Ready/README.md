# Hussain Events

Lightweight Android WebView app for Android 10+.

## Cloud build
GitHub Actions builds a debug APK automatically whenever code is pushed to `main`, or manually from **Actions → Build Hussain Events APK → Run workflow**.

The APK is available under the workflow run's **Artifacts** as `Hussain-Events-APK`.
